// Workspace settings
{
	    // The following will hide the js and map files in the editor
	        "files.exclude": {
			        "**/*.pyc": {"when": "$(basename).py"}, 
				        "**/__pycache__": true
					    }
}
